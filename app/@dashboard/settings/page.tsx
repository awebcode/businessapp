import { wait } from "@/app/actions/wait";
import React from "react";

const page = async () => {
//   await wait(2000);
  return <div className="h-[600px] w-[600px] rounded-md bg-green-500 m-10">Settings</div>;
};

export default page;
